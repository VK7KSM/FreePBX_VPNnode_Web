name = "freepbx-vpnnode-web"
main = "_worker.js"
compatibility_date = "2024-01-01"

# 可选绑定 KV (如果绑定了 KV，数据永久保存在云端；即使不绑定 KV，面板也能正常在内存中运行)
# [[kv_namespaces]]
# binding = "SUB_STORE_KV"
# id = "your_kv_namespace_id"
