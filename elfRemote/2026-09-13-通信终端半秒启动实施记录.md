# 2026-09-13 通信终端半秒启动实施记录

## 验收目标

用户要求继续修改直至达到点击到真实功能启动不超过500毫秒。PTT按设备开始播放有效音频计，电话按双向实际音频计，拍照按完整新照片显示计，录像按新视频首帧显示计，麦克风按实际音频抵达播放计。连接提示或按钮反馈不替代以上指标；冷启动、预建完成后激活、断网重连分别保留实际时间，不能混为通过。

## 已查明的等待

旧链路每次点击重新创建云会话、等待设备推送领取、建立两侧WebSocket、初始化原生媒体库及音频模块、调用SFU创建和发布订阅、建立ICE。Cloudflare参考资料也记录初次ICE连接约1.8秒的可能等待。因此需要在选中设备时预建传输，点击后只进行当前操作激活，不能通过修改显示文字消除真实等待。

拍照还有固定600毫秒预览等待，以及先上传R2、再通知网页刷新历史、最后网页取回照片的串行路径。拟将人工拍照改为等待实际首个预览帧，不再固定等待；先沿现有受保护控制链发送同一照片的即时内容，R2仍按原合同保存并核验，不能用缓存旧图冒充新照片。

## 兼容和资源边界

- 仅新增显式支持预建的候选能力，旧客户端、D31正在验收的旧协议及其它设备保持原入口；先更新与测试D22-XX，不自动扩展JJ或BB。
- 复用当前媒体创建接口、会话身份、WebSocket与SFU，新增预建/激活/停止本次操作的协议状态。预建只建立传输与空媒体源，禁止提前打开麦克风、相机或扬声器，禁止改变音量及焦点。
- 所有操作带递增编号，晚到的就绪、照片、结束消息必须绑定原编号；重复/跨操作消息不得激活另一次操作。停止要先关闭本次采集及播放，完成恢复后才允许下一操作。
- 选中设备且页面可见才预建；切换设备、隐藏页面、退出或连接失败时释放。预建与业务均有期限；减少无内容的控制心跳，保留断线检测。不得以全部设备永久唤醒或持续采集满足时延。
- 客户端确认资源释放失败时关闭整条预建通道，保留错误和恢复快照，不继续复用不确定现场。

## 当前进展

双向电话就绪时序修正已提交并部署，提交d43a901，432项网页测试通过。预建传输、即时照片和对应客户端尚在实现，半秒目标尚未验收。D31旧协议PTT仍由D31_dev按明确独占窗口继续，双方不同时操作其设备。

## 2026-09-13 用户明确流程更正

用户改为设备名称后显示连接/断开，由用户点击连接才建立持久媒体通道；停止具体功能保留待命，点击断开才主动释放。此前“选中设备自动预建”和“隐藏页面自动断开”的设想不再执行。新合同见同目录《2026-09-13-通信终端持久连接协议.md》。真实断线、切换设备和页面退出仍完整释放，单次功能期限与连接寿命分开。

只读XX第176版现场日志确认，用户本次PTT的多个播放窗口均为rms=0、peak=1，电话下行也近乎静音，而设备通话采音有明显非零数据；当前闲置音乐流为静音，符合前次恢复基线。由此确认不能用旧连接就绪替代真实发声，下一步核查本机输入选择与有效音频，不能单凭闲置静音认定扬声器故障。原始私有只读结果保存于.wrangler/media177-xx-readonly-1789258832521/；首次完整AudioFlinger读取结果因接口16000字符截断，未用于完整媒体日志结论。

## 2026-09-13 第177版XX候选上机预登记

目标为Wi-Fi下D22-XX，当前第176版。唯一部署范围是XX，经既有elfRemote OTA升级，不使用USB或ADB安装。候选版本0.1.177-production-media，18718593字节，SHA256=b41893659b7dfa35fb164d927583b07299d6a6765f8ed7d989f02277dd6ee21d；与既有发行证书一致。冻结制品和元数据位于Web仓库私有.wrangler/media177-release-20260913/。

验证假设：同一媒体传输会话可从待命激活操作、停止并确认硬件和路由恢复、再激活下一操作，不重复建立SFU或ICE。实际待命硬件空闲及半秒指标尚未通过，不扩展发布到JJ或BB。先核验安装健康和自然上报，再待Web部署窗口允许后进行媒体测试。

预期结果为仅XX版本与安装健康回执匹配；不匹配或安装失败停止测试，保全更新回执，使用既有更新恢复机制核验旧包保留。媒体故障立即关闭会话并检查音频/摄像头释放；如需回退功能代码，依据保留176源码构建同签名更高修复版本走既有OTA，不假设Android允许直接降级覆盖。

网页服务端部署暂等D31_Dev当前网络重启恢复原号归档完成，避免部署重载污染其现场。

## 第177版真机结果与第178版修正

第177版已仅XX完成OTA和health-ok，未更新JJ/BB。Web提交06b7eaa已上线，生产三份相关静态资源与本地SHA一致；446项Web、258项Android既有测试通过，不等于新增真机性能通过。

生产UI实测发现标题栏连接按钮使用了后面才赋值的媒体控制对象，已修正声明顺序，并新增实际renderRemoteConsole渲染回归。此前测试只覆盖控制器返回HTML，没有覆盖调用页首次渲染，属于测试缺口。

随后通过现有公开控制器方法进行同一XX候选的单次协议验证（未冒充按钮UI验收）：双方发布成功且ICE连接成功，空媒体源在订阅阶段失败。全部PC与WebSocket已关闭，浏览器没有申请麦克风或创建播放上下文。原件位于.wrangler/media177-protocol-connect-result.txt及设备只读结果.wrangler/media177-xx-readonly-1789260400751/。最初两次界面准入失败均未发起媒体会话，分别为新能力尚未重新上报及按钮声明顺序错误；原输出保留。

第178版通过软件静音和黑帧预备真实可订阅轨道，禁止提前采集硬件；具体内容见持久连接协议追加说明。硬件空闲、实际出声、切换和五百毫秒指标仍未验收，继续仅XX候选。

第178版上机预登记：仅D22-XX，当前177；沿既有OTA安装0.1.178-production-media，18718589字节，SHA256=5024315c9c009136333f417f7ea51a420f464c91b9fd24f4d0e4fc4acde47e48，与既有发行证书一致。唯一待验证变化是软件占位轨道是否让预备订阅成功，仍不采集设备硬件。先仅建链与空闲硬件检查，不直接同时启动六种功能。任一失败完整断开，保留本版全部输出和设备日志；不扩大JJ/BB。既有258项Android测试通过，网页447项通过，嵌入核对与静态构建通过；这些不替代真机结果。
