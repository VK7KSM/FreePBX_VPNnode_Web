# 媒体重复启停与回声修复

## 已确认问题

179设备日志显示同一连接首次PTT有播放及就绪，第二次无播放，直到服务器启动超时。180仅保留接收音轨仍复现；181在显式连接期间保留静音输出线程后，连续三次操作通过。首次连接还有串行等待：先等设备上线，随后才开始浏览器协商；两端SFU新会话请求与本机初始化也曾串行。

网页每次PTT重新申请麦克风及替换音轨。已有AEC请求不足以代表声学回声验收；原播放经Web Audio输出、自动增益开启，设备电话采样有满幅削顶。

## 公共网页修正

- 点击连接时一次准备系统默认麦克风；待命禁用原始音轨，PTT和电话激活才启用。停止单项回到静音；断开释放采音及全部处理节点。保留手动麦克风选择，每次新连接恢复系统默认。
- 原生音频元素承担远端播放，让浏览器AEC取得播放参考；波形分析只分析，不重复播放。PTT不播放设备回传音频。
- 双端软件AEC及降噪保留，关闭自动增益。网页发送侧加入高通、限幅及持续窄频啸音抑制；连续强尖峰才触发窄带滤除和短暂衰减，不以整体静音替代语音传输。
- 预连接两端独立开始初始化，音轨通知排在hello之后；仍按双端发布、订阅及ICE状态判断就绪。SFU创建与本机初始化并行，发布后先等ICE连通再通知对端订阅，避免过早订阅。

## 验证与边界

450项现有网页及新增协议回归通过，包含旧协议、重复PTT复用、迟到麦克风授权清理和预连接消息顺序。浏览器实际音频图的持续单频输入被抑制超过15分贝；静音和均匀宽频输入不触发窄频检测。合成测试没有连接电脑扬声器，不能代替真人语音及不同房间声学条件验收。

181在XX同一连接三次PTT均就绪并回到待命，只申请一次浏览器输入；就绪约578、716、732毫秒，尚未达到500毫秒。继续第182版优化每次路由和音量冗余设置，并增加首个非零播放缓冲时刻。不得把就绪回执写成实际扬声器声压或真人听感验收。

D31仅复用公共Web合同及参考说明，由D31_Dev独立适配及验证自身音频守卫。本任务不操作D31设备。


## 晚加入连接的订阅竞态补充

预连接缓存中区分已经创建的音轨和对端已确认可订阅的音轨。晚加入端不能仅凭发布接口已返回就提前拿到音轨；等待原published确认后再通知。补充回归覆盖hello顺序、无确认时拒绝订阅、确认后的正常分发，兼容旧协议。


## 本机麦克风就绪与静音门补充

浏览器禁用原始音轨可能让操作系统关闭麦克风硬件，重新启用仍有延迟。修正为显式连接期间本机原始采音保持就绪，发送音频图入口的静音门默认关闭；PTT/电话打开静音门，单项停止关闭静音门，完整断开释放原始采音。设备端麦克风仍只在实际采集功能中开启。

实际浏览器音频图验证：原始音轨启用时，待命处理输出RMS为零；激活后输出非零；持续强单频被窄带滤除，静音与均匀宽频不触发。原件.wrangler/media185-input-gate-result.txt。451项网页测试与嵌入校验通过。此项更正上文原始音轨禁用的实现，避免隐藏硬件重启。

## 第187版验证记录（2026-09-13）

最终客户端把媒体网络握手与本地音频准备并行，预建一个ICE候选池，并在显式连接期间维持静音输出线程与已配置路由。单项停止只关闭采集/发送门；完整断开恢复原模式、扬声器、音量及音频焦点。占位音频、真实采集及黑帧统一使用单调时间基，避免深睡后的媒体时间轴跳变。

XX实际187版：完整连接后的三轮PTT为138/126/126毫秒；连接中取消后重连，再三轮PTT为144/139/144毫秒；电话分别347/340/378毫秒。没有复现重复PTT启动超时。初连分别5982、3897、5143毫秒，仍有明显波动，尚不能声称初连即时；186候选池单独验收为4675毫秒，ICE阶段约130毫秒。

真实默认麦克风读回：AEC与降噪开启、AGC关闭、48千赫兹单声道。电话远端音轨实解码PCM非零；纯合成音及解码数据不代替真人语音或全部声学场景验收。连接取消测试的统计对象曾指向已关闭的首个PeerConnection，因此该轮空的callStats不作为电话无声证据，另用独立电话解码测试验证。

断开后路由快照为空，音频焦点栈为空，原媒体和通话音量恢复，音频输出待机且活动播放轨道为零。初次全量系统诊断超过任务文本上限，已另取有界输出确认清理结果，没有以截断内容代替验收。

证据见工作区.wrangler/media187-production-repeat-result.txt、media187-cancel-reconnect-result.txt、media187-call-decode-result.txt、media187-local-mic-result.txt，以及media177-xx-readonly-1789272028586/result.json。Android单元测试及构建通过；451项Web回归、嵌入检查与静态资源构建通过，生产页面和媒体脚本摘要一致。初连性能、其他网络和真人听感继续单独记录，不扩大为全机型通过。

## 第188版与独立媒体唤醒（2026-09-13）

188将音频模块、EGL和编解码工厂预载到空闲阶段，未创建PeerConnection和实际音轨；收到有效连接邀请后才开始网络候选与播放准备。服务销毁通过shutdown释放空闲引擎及执行队列。XX安装健康后、尚未建立媒体连接时，IPv4/IPv6应用UDP套接字均为零，音频输出待机、活动轨道为零，路由快照为空。实际连接日志的路由到工厂阶段仅2毫秒，证明使用了预载结果。

另修正媒体邀请误用普通状态请求合并的问题：每个新媒体会话产生新的唤醒代次，不被正在定位/上报的上一条请求去重。普通拉取保持原去重间隔；外部request-status不能伪造wake_key绕过去重。新增端到端回归覆盖旧报告未完成时新媒体立即唤醒、快速取消后创建新会话和普通请求仍合并。服务端提交1849e17，453项测试与生产部署成功。

XX188初次连接3915毫秒；独立唤醒部署前取消重连6376毫秒，部署后同条件4477毫秒；独立电话连接4900毫秒。PTT约131–151毫秒，电话321–421毫秒，实际解码音频非零。仍不称初次连接为即时，也不能保证所有网络都低于五秒。断开后重新检查应用UDP套接字为零，活动播放为零，路由恢复；预载引擎没有维持媒体网络会话。原件为.wrangler/media188-production-repeat-result.txt、media188-cancel-reconnect-result.txt、media188-wake-cancel-result.txt、media188-call-decode-result.txt及media177-xx-readonly-1789273421211/result.json。

## 最终JJ蜂窝复核（2026-09-13）

JJ通过Wi-Fi升级188后，独立900秒恢复窗口下再次关闭Wi-Fi，报告确认cellular。初连3647毫秒；同连接三轮PTT为96/136/169毫秒，电话299毫秒、麦克风322毫秒且都获得非零解码PCM；照片预览2183毫秒并已渲染；录像实际首帧938毫秒，分辨率240×320；警报短时启动307毫秒后停止。设备前三次非零播放缓冲为31/82/66毫秒，全部操作结束，无启动超时。照片、录像首帧及首次连接仍未达到500毫秒，保留为明确性能差距。

这轮应用蜂窝接口计数增量为接收111009字节、发送398843字节，合计约498KB，包含主动照片、录像与诊断；不能折算成日常静置用量。完成后恢复Wi-Fi，维护健康，音频路由快照为空；独立测试计时进程结束，浏览器media180已关闭。浏览器音频处理和设备播放/解码的自动化证据不代替不同房间、音量及真人声音下的最终听感验证。

原件：.wrangler/jj188-media-all-result.txt，以及elfRemote/captures/2026-09-13/jj188-media-1789273667262/。XX、JJ当前188；本轮未操作BB或D31。D31_Dev已收到公共Web独立唤醒、音频静音门及已验证D22实现的交接，由其独立适配。
