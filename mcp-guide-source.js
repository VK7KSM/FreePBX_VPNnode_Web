export default "# elfRemote MCP 操作说明\n\n这份文档有两个读者：连上来的 AI（它作为 MCP 资源自动读到这份内容），\n以及在设备管理页发令牌的人。工具参数表里塞不下的东西都写在这儿。\n\n## 一个令牌只管一台设备\n\n令牌在网页上生成时就绑定了某一台设备，所以工具参数里**没有 `device_id`**，\n也不需要你选设备——你连上的这个服务器就是那一台。\n\n要管多台就在 MCP 客户端里配多个服务器，名字里带机器名：\n`elfremote-d22-bb`、`elfremote-d22-jj`。这样同时开着也不会搞混。\n\n## 先看能力位\n\n任何排查都应该先调 `device_info`。返回里的「能力位」决定此刻哪些操作真的可用：\n\n| 能力位 | 决定什么 |\n|---|---|\n| `managed_exec_tasks` | `run` 能不能用。为 false 时下发必然失败 |\n| `managed_log_tasks` | `pull_logs` 能不能用 |\n| `managed_media` | 通信终端是否可用。**它反映设备上那份音视频原生库在不在**，不是写死的 |\n| `managed_desktop_v1` | 远程桌面是否可用，同样取决于原生库 |\n| `managed_update` / `managed_update_v2` | 能不能下发更新 |\n\n能力位是设备自己上报的实测结果，不是配置项。`managed_media` 为 false\n通常意味着那台机器上的原生库还没到位，不是权限问题。\n\n## 任务是异步的\n\n`run`、`pull_logs`、`files`、`system_config` 都是**下发任务**：调用立即返回一个\n`task_id`，不等结果。拿 `task_id` 调 `task_status` 取结果，通常三到十秒就有。\n\n这么做不是偷懒：整台设备的请求在服务端是串行处理的，在调用里等三十秒\n等于把这台设备的所有流量一起卡住。\n\n一次 `run` 只跑一条命令。要跑多条就调多次——哪一条失败一目了然。\n\n## 版本下发顺序，这条踩过坑\n\n同一个版本码会有两个包：\n\n- **完整包（`variant: full`，约 12.4 MB）**：自带那份 12 MB 的音视频原生库，供首次装机\n- **更新包（`variant: slim`，约 0.8 MB）**：不带库，做日常 OTA\n\n设备第一次装上完整包时，会把原生库解到自己的数据目录里持久保存\n（`getFilesDir()/media-native/<abi>/`，跨 APK 替换存活）。此后更新就只需要更新包。\n\n**从没装过 0.1.204 及以上完整包的设备，不要直接下发更新包。**\n它装上之后本地没有库，会自己去服务器下那 12 MB——在 Wi-Fi 上只是慢一点，\n在移动数据上就是一笔冤枉流量。\n\n判断办法：`device_info` 看 `客户端版本`；不确定就先下发完整包，装完再用更新包。\n\n## 不可逆和会中断的操作\n\n- **`install` 会改变设备上实际运行的程序**，安装过程中设备短暂不可用。\n  下发前先用 `releases` 确认版本码与变体。\n- 服务端的清单签名、证书匹配、有效期与通道校验一条不减，和网页走同一段代码。\n  被拒绝时按返回的理由改，不要换个参数硬试。\n- 媒体（拍照 / 录像 / 麦克风 / PTT）、丢失模式、擦除**不提供给 MCP**，\n  要用请人去网页上操作。这是刻意的，不是遗漏。\n\n## 时区\n\n所有时间一律用悉尼时区（`Australia/Sydney`）表述。接口返回的是 UTC 或毫秒时间戳，\n写进报告、记录、对话时请先换算。\n\n## 令牌管理（给人看的部分）\n\n在设备管理页「功能设置」标题栏点 **MCP** 按钮，弹窗里新建。\n\n- **有效期只有 1 小时 / 12 小时 / 1 天 / 1 周。** 刻意做短——这是操作硬件，\n  不是操作程序。过期重新生成即可。\n- **明文只显示一次。** 关掉弹窗就再也看不到，服务器只存哈希。丢了只能删掉重建。\n- 创建后弹窗给一条 `claude mcp add` 命令和一段 JSON 片段，二选一，都带复制按钮。\n  **不要把令牌贴进聊天框**——那等于写进日志。\n- 权限逐项勾选。默认勾上读状态、执行命令、拉取日志、查任务、列版本、下发更新。\n- 列表里能看到每个令牌的最后使用时间与调用次数（最多滞后 1 分钟或 20 次）。\n  你没让它干活它却在动，就该删掉。\n- 从单设备分享页生成的令牌，**删除那条分享链接时会连带吊销**。\n\n## 认证细节\n\n令牌只从请求头读：\n\n```\nAuthorization: Bearer <64 位十六进制>\n```\n\n绝不接受放在 URL 查询串里——URL 会进各种访问日志。\n\n令牌失效时服务端回的是人话（「令牌已过期，请在设备管理页重新生成」），\n不是干巴巴的 401。看到这句就是去网页上重新生成，不要反复重试。\n";
