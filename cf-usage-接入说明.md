# Cloudflare 用量导航组件接入说明

记录日期：2026-09-12。记录人：D22_Web_Dev。

## 实现和显示口径

- `cf-usage.js` 采集账号范围内全部项目的 Workers 请求、KV 读取/写入/删除/列举、DO 请求/读取行数/写入行数，以及 R2 标准存储的月度 A/B 类操作与当前容量。不会只统计本项目而误判账号额度。
- `cf-usage-client.js` 提供独立样式、导航标记和浏览器脚本。摘要宽度 232 像素，显示 Workers、KV 读取、DO 请求的免费额度占比，以及 R2 当前容量；展开面板给出各指标、周期和更新时间。保留蓝灰字体风格，黄色表示接近额度，红色表示达到参考额度。
- Workers、KV 和 DO 按 UTC 日统计；R2 操作按 UTC 月统计。R2 当前存储容量不是月度平均存储账单，不拿当前容量除以免费月均容量来伪造账单百分比。
- 用量来自 Cloudflare 分析接口，存在延迟及自适应抽样。它是免费额度参考，不是完整账单；此模块没有声称覆盖尚未取得的 Realtime、DO 计算时长等指标。
- 接口无权限、响应缺字段、未知 R2 操作类别和查询被截断均显示未知或带周期的旧值，不补零。健康查询返回空操作数组时，才可报告该项零操作。

## 最终生产接线

- 三页导航使用统一标记、样式与静态脚本；`GET /api/cf-usage`经过原管理员鉴权后直接读取KV快照，不进入设备DO。
- 最终采用`.github/workflows/cf-usage.yml`每15分钟调用`scripts/collect-cf-usage.mjs`，复用项目已有GitHub发布凭据。公开仓库的标准Linux任务不消耗私有仓库免费分钟额度；调度可能延迟，界面始终保留实际采集时间。
- Worker只配置`CF_USAGE_ENABLED=1`并读取快照，未加入分析或部署密钥。原模块保留可选采集函数，但本轮没有在Worker定时入口接入，也不会双重采集。
- 快照只保存聚合值，不含账号标识、桶名称、原始业务数据或凭据。当前用免费方案作比较；升级套餐后应同步修改显示口径。

## 消耗和故障处理

正常一天至多96次计划采集、288个分析请求、96次快照写入，另各一次REST读取旧快照。采集不依赖浏览器或开发电脑在线。页面正常每15分钟请求一次快照，可复用浏览器缓存；Worker实例内并发合并并缓存五分钟。后台页面暂停读取。

采集异常保留上次数据和所属周期，退避由共享快照记录；接口失败遵守Retry-After。KV不可用时页面和Worker各自保留已取得的快照，不能在Workers停止执行期间伪称仍能获得实时统计。

## 验证

独立模块九项测试通过，并已用内存存储替身进行真实只读采集，十一项指标取得数据。页面集成、鉴权、排版和正式计划任务的结果由主任务随后追加。

## 2026-09-12 晚间：修复定时采集数小时不更新

记录人：D22_Web_Dev。本节替代前述“最终生产接线”中的单一 GitHub 调度决定，旧记录保留作为历史证据。

- 已确认：GitHub 工作流虽然配置每十五分钟执行，实际自动运行时间为 UTC 01:32、06:28、11:13；悉尼 16:28 至 21:13 没有新的自动采集。因此用户 20:34 看到 16:28 快照是采集调度中断，不能归咎于浏览器。
- 修复：现有 Worker 每分钟定时入口在十五分钟边界启动统计采集，独立于设备 DO 初始化和清理任务；不新增定时触发器。GitHub 保留外部备用，复用共享快照的下一次采集时间跳过重复请求。KV 最终一致性及罕见并发仍可能产生少量重复，不能宣称严格全局恰好一次。
- 权限：生产 Worker 新增仅允许本账号统计读取的独立凭据，已实测十一项指标完整返回。部署凭据没有放入 Worker。公开代码、静态网页和快照均不包含密钥。
- 故障：部分指标缺失时保留对应旧值和标记，正常指标仍每十五分钟更新；全部指标失败才指数退避。浏览器请求二十秒超时后释放等待状态，后续可以恢复读取。
- 消耗：正常仍为每天约九十六轮、二百八十八次分析请求及九十六次快照写入；备用调度增加的是检查旧快照的读取，正常跳过分析及写入。主采集复用已存在的定时执行，不新增设备查询。
- 验证：新增生产入口在 DO 初始化失败后仍完成采集、部分失败保持十五分钟、备用采集跳过重复、浏览器超时恢复测试；全量四百二十六项测试通过，嵌入检查与静态资源构建通过。
- 边界：十五分钟是后台采集周期，浏览器可见且登录时也每十五分钟读取，叠加缓存和统计服务本身延迟，不保证刚到整点页面立即变化。整个平台停止运行或 KV 不可用时仍只能保留最后快照。

### 生产自动采集验收

提交 `879e086` 已推送并部署，GitHub 部署任务 `34690927510` 成功。UTC 11:24:14 生产五项静态资源与本地构建哈希一致。悉尼 21:30:26，生产 KV 快照自动生成，`status=ok`、`failures=0`，下一次采集时间为 21:45；同期 GitHub 采集工作流最后运行仍为 21:13，没有新的自动或手动采集。由此确认本次快照由修复后的 Worker 定时入口更新，完成首次真实自动采集验收。
