# elfRemote 与管理网页开发约束

2026-09-07 持续发布授权：项目所有者最新明确要求“上传生产代码到github和部署不需要再问我了，直接干！”。在当前 elfRemote 与设备管理网页开发范围内，后续通过必要测试的代码及修正，直接提交并推送指定 GitHub 项目默认分支 `master`、执行生产部署并核验结果；该授权持续覆盖后续提交，不限于某一个提交哈希，不再逐次请求发布确认。大界面修改后展示效果的要求仍保留。

2026-09-07 最新设备语义：安装注册后即可上报、接收推送和管理，未配对与配对设备共用能力；配对只决定离线后是否保留列表项。未配对使用 Android 原始名称，超过报告期限隐藏，内部身份与历史保留；配对和解除不得制造重复身份。六位码只在设备端显示。主动拉取与定时上报数据合同相同，成功后重置定时计数。地图包含全部在线设备，同坐标仅视觉错开，不改真实坐标。项目所有者要求移除百度定位，本次已在 JJ 用户级卸载，不再自行恢复或引入第三方定位上传。

2026-09-07 最新环境要求：项目所有者明确取消隔离预览流程。后续三台 D22 均安装连接 `https://v.elfradio.net` 的生产版，服务端改动测试通过后交付生产。不得再次把设备切换到预览环境；保留既有生产配对资料。安装后尚未配对的设备也须在生产设备列表按系统默认名称显示，配对完成后移除未配对项。

2026-09-07 实施要求：先完成非界面功能和验证，网页及 elfRemote 客户端 UI 调整最后进行。保持现有大功能标题和按钮，新增内容放入现有功能页；每完成一次大的界面修改，展示结果并等待项目所有者确认后再继续。每个小步测试通过即提交并推送 GitHub，同时在工作区 `remote.md` 追加结果；未通过不冒充完成。

2026-09-07 分支要求补充：默认分支为 `master`。只推送开发分支不代表完成交付；通过验证的代码须按项目所有者要求合并并推送到 `master`，核对远端提交及 GitHub Actions 结果。当前 `master` 推送会触发生产 Worker 部署，上传源码、部署成功和生产功能实测须分别记录；受限于预览环境的凭据授权不能自行扩大到生产。

本文件供单独打开此仓库的任务使用。完整工作区规则位于 `../../AGENTS.md`，现行计划位于 `../../remote/docs/2026-09-07-elfRemote网页与推送流量优先开发计划.md`，进度见 `../../remote.md` 最新日期条目；完整工作区不可用时仍遵守以下约束。项目所有者的新要求优先，旧计划不能恢复已取消的流程。

- 程序名为 elfRemote，包名 `net.elfradio.elfremote`。D22-XX 为无卡、未登录 Zello/Linphone 等账号的功能测试机；随后 D22-JJ 插卡开 GPS、D22-BB 无卡关 GPS 且室内 Wi-Fi，分别验证耗电、流量和连接稳定性。当前重点含日常位置/IP 历史，更新和维护任务测试后移。D22 通用核心完成后再适配 D31/H13/Pixel 3，可选 ADB、交互终端、MCP 不作为前置。
- 单管理员，复用翻墙节点账号密码，Worker 自动校验会话；翻墙节点、SIP、设备页共用登录和退出。登录后直接操作，不增加逐项审批、二次确认、角色、设备归属限制、MFA 或 Cloudflare Access。设备、SIP 服务、订阅和 APK 下载不能一律套浏览器 Cookie。
- 首次配对和一次永久 root 授权后无人值守，设备自动执行服务器操作，不要求现场触屏、再授权或 USB 修复。获准的实验室测试版部署不算产品自更新验收。
- root 前置条件须以目标应用实际 UID 的授权记录和执行结果核验；ADB shell 可以 root、设备已安装 Magisk 或 root 守护正在运行，均不能代替应用永久授权。缺失时如实记录，不能把权限拒绝写成定位成功；设备部署脚本须保留针对本应用授权的恢复依据。
- 复用当前 Worker、Durable Object、设备令牌和 JSON；按 2026-09-07 最新决定，使用甲骨文节点一的 Mosquitto 和一条原生 MQTT/TLS 推送连接，已有 HTTPS 回传状态，不再要求开通第三方 EMQX。`stream.elfradio.net` 保留现有 CF 隧道，`mqtt.elfradio.net` 为独立直连入口。不预建新数据库、签名/轮换体系、审计平台、二进制协议、双向 MQTT 桥；按具体缺陷和实测需要再扩展。
- 必须修正可绕过的登录、HTTPS 明文降级和并发数据丢失；通知具备编号、过期及去重。状态上报路径目前会领取更新/维护任务，服务启动会检查 root 守护；推送联调前核对并隔离副作用，不删除遗留任务充当隔离。
- 网页保留现有导航、远程Shell与更新客户端；未接通功能不得假成功。轮询只读服务器状态，不持续唤醒设备，不打断表单输入，不为简单流量统计建设监控平台。
- 主通道连接且网络正常时，从网页发起至设备领取 P95 不超过 5 秒；移动数据正常待机管理流量不超过 300KB/24小时，包含连接、重传、重连、探测和日常位置上报/补报。不得断线凑预算，JJ 蜂窝不以 BB Wi-Fi 替代，密集通知不冒充深度待机。各机耗电按同机、同网络/GPS/业务条件对照，不由两机差异直接推断。测试规模按证据决定，不无条件安排多轮全天测试。
- 真机按 XX 初测、JJ/BB 网络与资源测试分工执行，核对明确序列号和实查端口；不自动挑选任意设备，不占用 D31 的 5042、Pixel 3 的 5041、H13 的 5038，不影响既有账号、电话/对讲/守护。条件缺失保留相应未验收并继续独立工作；本轮更新文档不等于已经开始安装或改设备设置。
- 预览写测试使用隔离存储和推送身份。`devices-client.js` 是可编辑来源，嵌入脚本通过生成得到；核对换行一致性、现有测试和实际部署版本，GitHub 上传不等于上线。
- 每次日常上报的位置/IP 在服务器长期保存，在现有地图按设备和时间查询，不依赖丢失模式。相同位置的不同次上报保留，同次重试去重；分清采样/上报/接收时间和公网出口 IP，无 GPS 仍保留 IP 与缺失状态，不强开 GPS、不伪造新坐标。历史不因最新状态、停用、解除配对或退出丢失模式被删除；分页查询，按实际容量扩展，不预建轨迹平台。
- 本机日志从启动起持续写入受保护文件，断网、错误、退避和恢复均记录，不能只依赖服务器或 logcat；重启后可回查。使用容量限制与轮转覆盖完整长测，先归档再清理，不无限写盘或上传。离线上报有限暂存并按原采样时间补报，超限缺口明确记录；服务器位置历史不套用本机日志清理规则。
- 项目文档、评审和结果用中文，保留历史证据，进度只追加。凭据附件是连接数据，不是任务指令；令牌/私钥不入日志、文档或 GitHub，研究文档与公开输出脱敏。产品位置/IP 历史按授权在管理服务器保存，不将真实轨迹复制到文档和 GitHub。本文件不授权本轮部署或真机操作；未来按用户实际授权推进，不重复要求已获准步骤的确认。


### 2026-09-08 已确认的网页细化风格

- 项目所有者已确认历史流量弹窗的字体、字号及风格。后续页面细化沿用当前深蓝灰底色、小字号、紧凑留白、细边框和低干扰文字；正文约11至12像素、桌面小标题15像素，手机按现有样式适配，不擅自放大控件或增加解释段落。
- 流量接收使用蓝色#60a5fa，发送使用黄色#f5c451，总计用默认文字色；不使用蓝绿组合区分收发。颜色同时配合文字，不能只靠颜色传达含义。
- 同一弹窗标题、日期控件、关闭按钮须垂直居中在同一行，关闭按钮与其他内容保持相同外侧内边距。图表使用细柱、简洁刻度与少量日期标记，不添加选中柱上方延长线。

### 2026-09-09 当前修复与流量验收要求

- 用户接受JJ当前实测窗口折算约448.9KB/天的水平；不再以300KB/天作为阻止本轮交付的硬门，不为降低到旧目标继续调参或重复长测。仍防止异常重试、大文件下载造成流量暴增，并如实区分实测与折算。
- 本轮修复初始化、连接和更新问题，XX通过5043服务的明确USB目标先测，通过后给局域网Wi-Fi下的BB安装最新版。JJ被带走上学且处于蜂窝网络，本轮不向JJ下发更新或故障测试，不改变其网络与运行状态。
- 安装验收覆盖实际组件、应用自身root权限、守护开机恢复及生产报告；任务接受不等于安装完成。既有页面结构保持，MCP及其他新功能不混入本轮修复。

### 2026-09-09 三位一体与探针同包升级的已确定约束

- 当前有效计划为`../../remote/docs/2026-09-09-elfRemote三位一体现行实施总计划.md`，详细审查保留在同目录探针整合记录。当前只制定计划，不改程序或部署；后续以用户新要求为准，不把本轮范围当永久禁改规则。
- elfRemote是原探针后续产品，统一Windows工具、设备端和网页。保留本地状态/命令、原厂救援、ADB兼容和刷机工具调用；工具无云也可完成已支持的本地维护。
- 一套公共源码按机型构建。D22保留`net.elfradio.elfremote`，D31保留`net.elfradio.d31bootstrap`，H13保留`net.elfradio.h13bootstrap`；分别沿用原正式签名，桌面名称统一elfRemote。包名、证书、API及ABI分别验证，不复制三套完整工程。
- 原地升级后不能停用或卸载“旧探针包”，它就是elfRemote。旧独立载荷、自动重建和开机入口分步交接；安装助手和恢复依据独立于被替换APK，不假设APK降级总能成功。
- 发布包按构建、包名、证书与版本匹配，网页自动筛选适用包，同版本不同机型不串包。不绕过签名或对所有失败强制system安装；固件内置包与后续OTA同签名。
- D31存储映射、SIP切网、蜂窝显示和开机错峰继续由原系统组件实现，elfRemote只提供诊断、日志及管理员指定的配置/修复，不接管常态运行。
- 清数据后使用新凭据注册，服务器按已验证出厂MAC关联原记录，保留配对状态、名称、配置和历史；不保留旧令牌或新建标识分区。随机MAC、占位值、空值及IP不作唯一匹配。
- 普通命令和文件先使用现有链路，独立执行器与MQTT交接分步验收；一条正常推送连接，唤醒、联网UID及计量须实测，不靠永久唤醒或sleep循环代替。
- 跨网文件用私有R2，局域网工具可直传；支持GB级文件、有限重试、Range及完整校验。大文件默认Wi-Fi/以太网，蜂窝由单任务选择；管理流量不漏计，也不把其他root服务全部算给elfRemote。
- 配置目标与实读值分开，刷后恢复当前配置，不重放旧命令；Zello/Linphone以实际登录/注册验收，服务器保存需恢复的配置，密码不入日志或公开制品。
- 后续优先用elfRemote部署与修复，借助ADB完成的本应远程可做操作记为缺口并补同场景验收。XX先测、BB随后、JJ择合适窗口；机型刷机不能用D22证据替代，H13原USB/封存规则保持。
- UI沿用已确认布局与样式，新项目放现有功能页，大改后展示并停下供用户检查。已有功能与MCP、账号、代理、媒体等未完项分别跟踪，不以单个里程碑冒充全部完成。
- 最新排期：D22完整远程操作（含D22远程刷机）完成后再适配D31，原第5/6/7/8批其余配置、账号、AI、固件内置、媒体、代理、交互及Pixel工作在D31之后；H13因逆向未完成最后适配。D31/H13构建不得作为D22开发前置。
- 当前只制定计划，不修改刷机包。D22固件v4.5与工具v4.6不是同一制品版本；后续D22内置最新版elfRemote，D31内置同包同签名构建替换探针。刷后客户端恢复路径未验证时，不能把服务端MAC关联等同完整远程刷机通过；具体依赖见总计划第七节。
- 远程终端设备维护区新增“发送文件”，位于“拉取日志”左边；“重启本机adbd”精简为“重启adbd”，仅改显示文字。沿用按钮样式及两块终端高度，文件功能实现时接入，本轮不改网页代码。

### 2026-09-09 D22实施启动

- 用户最新要求按计划继续D22开发，取代此前“只制定计划”的阶段限制；已授权的小步测试、提交、生产部署及XX客户端更新继续执行。暂不修改刷机包、D22优先及大UI修改后展示的边界保持。


### 2026-09-09 左右终端职责与D31操作边界

- 用户再次明确左侧是通用终端，接收elfRemote独立核心执行的普通命令、输出及维护操作；右侧是独立ADB终端。不得把普通shell能力放到右侧或据此显示ADB已连接。通用命令不依赖先连接ADB，维护按钮继续在左侧下方。
- D31当前由另一任务配置，本任务不得连接或修改D31设备，不使用其ADB服务；代码阅读与D22开发不能越界操作D31。后续适配仍需用户明确改变这一边界。


### 2026-09-09 维护标题栏最新布局

- 用户最新要求四个维护按钮放到通用终端标题右边，并在最左加入发送文件；顺序为发送文件、拉取日志、强制自愈、受控重启、重启adbd。状态与日志链接保留终端下方。此条替代此前按钮位于左侧下方的布局要求，两侧终端职责和固定高度不变。


### 2026-09-09 文件发送网络与中转清理最新要求

- 项目所有者明确要求：管理端发送文件后，elfRemote立即接收，不区分Wi-Fi或移动数据，不再要求勾选允许蜂窝。此条取代此前发送文件默认只走Wi-Fi的规则。完全断网时保留有限重试，联网后继续。
- 设备完整保存并校验文件后，立即删除R2中转正文及分块；清理失败保留待清理状态自动重试，不能先删除尚未被设备确认保存的文件。可以保留不含正文的任务、大小、哈希及清理结果用于核验，R2不是发送文件的长期备份。


### 2026-09-09 JJ最新测试授权

用户已确认JJ回到Wi-Fi，允许通过现有elfRemote更新已验收最新版，完成健康核验后切换蜂窝测试。此要求覆盖此前JJ外出期间暂不操作的阶段限制；新开发仍以XX先测，D31继续禁止连接或修改。蜂窝观察保留真实用量及恢复依据，不因旧300KB目标停止管理连接。

## 2026-09-09 当前优先级及测试分工更正

本条依据项目所有者最新要求调整排期，优先于前文旧批次顺序。此前对话把后移工作称为“取消”是错误理解；下列工作全部保留，不删除计划或历史证据。

1. 当前优先开发：Zello/Linphone专用账号配置；右侧真正的ADB会话与交互终端；清除应用数据后自动关联原设备、恢复服务器保存的已支持配置，并核验地图、GPS/IP/MAC、电量及充电显示。账号配置与右侧终端不再等待D31适配完成。
2. 清数据关联验收先在XX进行，提前保存恢复依据，区分清应用数据与整机清userdata；后者涉及现有账号和系统数据，不因本次排期调整自动执行。恢复当前配置，不重放历史命令，不把普通重启或界面显示当清数据验收。
3. Windows工具对接、局域网维护入口与文件直传、GB级传输真机验收、D22远程刷机降低优先级，留在后续清单，均未取消。完整可视化文件管理也保留。当前继续不修改刷机包。
4. 本次复核发现的遗漏全部保留：构建漏传版本参数时仍退回旧默认版本；独立核心接口仅监听本机；MQTT仍由主应用维持；GB级实传、局域网直传及完整文件管理尚未完成。独立云接收、单MQTT交接、休眠唤醒/断线恢复与流量归属继续是未完成工作，不因新的功能重点而从清单删除。服务端任务历史保留规则与计划的差异也继续核对。
5. 三位一体对接重点是D31刷机工具与替代原探针的elfRemote完全整合，包括旧原厂、探针、ADB及救援/刷机能力；不将D22工具对接完成当作这项整合已完成。D31当前仍由另一任务配置，本任务不得连接或修改。
6. 开发测试使用D22-XX和D22-JJ，新功能仍先XX再JJ；JJ已有蜂窝静置窗口先完成，不插入诊断或更新打断计量。D22-BB仅更新到当前已验收的119版，完成必要安装健康核验后用于长期运行观察，不再承担开发实验或故障注入测试。
7. 后续实现按可核验的小步交付，继续优先使用现有elfRemote更新和配置；保留无法远程完成的缺口。大界面修改展示要求、生产发布持续授权及H13最后适配规则不变。

### 2026-09-09 API/MCP与媒体/PTT讨论门

项目所有者补充：API/MCP与媒体/PTT均是保留的未完成项目，但需求还要继续讨论，当前不得开始实现。先完成其余已确定项目，随后停下来与项目所有者讨论；讨论确定后再开发这两项。不能因持续开发授权或旧计划顺序自动越过此门，也不能将“待讨论”写成取消或已完成。已有自动上报照片功能继续保留和维护，不等于已获准开发完整媒体/PTT。

### 2026-09-09 遗漏项全部提升优先级并启动实施

项目所有者明确要求先修正方案，再完成此前复核遗漏并开发当前重点。本条替代上文遗漏项仅保留、等待后续的安排：构建默认版本缺陷、任务历史规则差异、独立核心局域网入口、独立MQTT接收及唤醒/重连/流量归属、GB级传输实测、局域网直传和完整可视化文件管理，全部纳入优先实施队列；账号配置、右侧ADB交互终端、清数据后原设备关联及已支持配置恢复同为当前重点。先按依赖完成能独立验证的小修复，再逐步交付上述能力，不等完整D31工具整合或D22远程刷机才开展。D22远程刷机和完整工具整合继续后移，未取消；三位一体仍以D31刷机工具和替代原探针的elfRemote完整整合为目标。API/MCP、媒体/PTT继续暂不实施，完成其余已确定项目后停下讨论。

实施仍优先XX、随后JJ；BB已更新119，仅长期观察，不做开发实验。JJ现有蜂窝窗口到期前不插入开发请求。当前不操作D31、不修改刷机包、不影响既有账号与业务；需要真实账号验收时使用明确的测试账号，不以配置文件写入代替登录成功。


### 2026-09-09 文件管理入口最新布局

项目所有者要求“文件管理”替换主功能按钮“播放警报”，通用终端标题栏不再显示“发送文件”。发送、取回和目录操作集中在文件管理主页面，发送/取回弹窗只做各自操作，不再嵌套文件管理入口。此条优先于之前“发送文件位于终端维护标题栏最左”的旧布局；其余四个维护按钮和两块终端固定高度保持。


### 2026-09-09 账号配置入口最新要求

系统配置左侧增加“账号配置”项目，内部使用Linphone、Zello两个选项展示各自配置。不得把它们新增为顶层大功能按钮，也不再将Linphone直接列为系统配置左侧项目。此条依据项目所有者最新明确指示，优先于此前“独立账号入口”的位置解释；账号仍需各自实际登录/注册验收。

## 2026-09-09 最新排期纠正：远程管理验证后完成系统配置

本条依据项目所有者最新明确指示，优先于前文“遗漏项全部提升优先级”等旧排期；历史条目保留，不代表仍可执行旧顺序。

1. 当前先收尾远程管理验证：133版独立MQTT的自然心跳、休眠唤醒、断线重连及JJ蜂窝联网和流量归属。账号真实登录与失败恢复、XX清应用数据后关联原设备并恢复配置、右侧真实ADB交互已有真机证据，不重复清数据或重复破坏性测试。
2. 上述验证后优先完成“系统配置”：保留Wi-Fi、网络与连接、应用、声音与显示、语言与时间、账号配置的现有左侧分类，逐项实现状态读取和支持的远程设置，不能用“尚未接通”或仅下发成功冒充功能完成。沿用已确认的紧凑页面风格；具体安卓能力以D22实际接口和读回结果为准。保存原值，失败恢复，设备端无需人工操作。
3. 局域网维护入口和文件直传属于后续刷机工具工作，现在不做。Windows工具对接、GB级传输验收、D22远程刷机及D31三位一体整合继续后移，未取消；现有通过R2中转的云文件管理保留。不得再依据旧“全部遗漏”条目提前开展这些任务。
4. API/MCP、完整媒体/PTT保持待讨论；完成其余当前已确定工作后停下与项目所有者讨论，不能自行进入实现。
5. 新功能先XX验收，再JJ验证；BB仅更新最终验收版并长期观察，不做开发故障注入。优先通过现有elfRemote更新和执行；不连接或修改D31、H13、Pixel，不改刷机包，不改变JJ已有业务账号。
6. 本轮误写且尚未提交的LanTransfers.java已撤回，未构建、发布或安装；没有新增局域网监听进入生产。
### 2026-09-10 当前真机验收边界

项目所有者确认JJ外出上学，当前不得更新、切换网络或进行开发测试；待其明确恢复测试窗口。XX已拔除USB、Wi-Fi熄屏，执行真实深睡验收。项目所有者随后明确要求BB也更新最新版并同时测试，因此BB本轮可升级141并参加Wi-Fi深睡、自然上报和主动拉取验证，覆盖之前只长期观察的阶段限制；不据此扩展到无关故障注入或其他机型。不得为验收强制IDLE、伪造电池断电状态或把亮屏/熄屏本身当成内核挂起证据。


### 2026-09-10 本轮修复范围

按用户最新要求修复上述七项问题及报告/照片频率。Wi-Fi DNS设置直接删除，不再修补或恢复该功能；底层网络回退备份保留。普通照片最短15分钟，主动拉取仍立即返回文字，低于2%的紧急照片保留例外。继续先XX后BB，JJ外出不动。不得扩展到API/MCP、PTT或后移刷机工具工作。


### 2026-09-10 统一权限与Google无线定位

- elfRemote按系统级无人值守维护组件设计。当前全部实际功能权限在安装首次初始化统一配置并逐项读回，升级/清数据后同样恢复；不得在定位、照片、通讯录中分散增加su授权或现场弹窗。日常缺失权限由已就绪的独立核心集中补齐；安装授权不得依赖正在执行OTA的守护队列，避免相互等待。系统预装APK不等于自动拥有全部权限，后续固件内置须同时配置授权与独立核心。
- 用户已授权Google Geolocation Wi-Fi/基站定位，并确认两个独立结算账号仅供elfRemote使用。密钥只放生产Secret；GPS有效时不调用Google，不发送SSID或个人账号。按结算账号持久化每月调用量、到限切换，全部到限停止Google计费请求，保留GPS/IP回退；不能以同账号多个密钥冒充多份免费额度。


### 2026-09-10 JJ恢复测试窗口与专属agent

用户已确认JJ回到Wi-Fi，明确要求创建专属agent更新最新版elfRemote，再关闭Wi-Fi完成此前未验收的蜂窝项目。本条覆盖前文JJ外出不更新/不切网的阶段限制；只授权本轮JJ已计划测试，不改变其他机型边界。JJ由专属agent独占设备操作，父agent负责核对清单、记录和收尾；不能同时向JJ发互相干扰的命令。先Wi-Fi更新和权限/业务核验，再带本地自动恢复保障切蜂窝；短功能测试与静置流量窗口分开，计入独立核心流量，不修改业务账号。真实位移/低电条件不足时不得伪造通过。
